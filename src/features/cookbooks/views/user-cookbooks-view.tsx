"use client";

import React from "react";

const UserCookbooksView = ({ username }: { username: string }) => {
  return <div>UserCookbooksView — {username}</div>;
};

export default UserCookbooksView;
